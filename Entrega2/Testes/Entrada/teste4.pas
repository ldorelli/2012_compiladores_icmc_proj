program inconstitucionalicimamente_esse_deveria_ser_Grande; { identificador muito grande }
{ declaracao de variaveis }
var x integer;
const x := 3; { constante no lugar errado }
var elementar, wattson : integer
var nakamino, uminishi: real;
{ declaracao de procedimentos }
procedure plaft(y : real, 2 : integer, integer : integer) ; { erro nos parametros }
begin
	x := 12;
	plaft(x, y, t, z, queijo?); { ';' em vez de ',' }
	begin
		begin
			if x <> 1898 then
				x := 3; { fecha o if, entao ha erro no else }
			else if x <> identificador
				x := plo(); { faltam parametros }
				repeat
					x := x + 1
				until x <> 2;
		end;
	end;
end;
end; { end a mais }

procedure sembegin (u:real); { assume que o primeiro begin eh do procedimento }
	u = 12.4; { serah ignorado }
end; { serah ignorado }

begin
	numeros (1, 23); { numeros nos parametros }
	naofecha(x; y; z) { Falta ')' e ';' }
	u := u - -1; { comando valido. -1 eh op_un e numero }
	procedimento( plaft(z, y, x), y, z); { um procedimento nao pode ser parametro }
	correto;
	x :+ 3
end.
